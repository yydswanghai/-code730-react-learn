import React from 'react';
import ReactDOM from 'react-dom/client';
import TickControl from './TickControl3'

const root = ReactDOM.createRoot(document.getElementById('root'))

root.render(<TickControl />)